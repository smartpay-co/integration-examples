{
  "name": "smartpay-integration-example-react-and-ruby",
  "version": "1.0.0",
  "description": "",
  "main": "start.js",
  "scripts": {
    "client": "cd client && #!/bin/sh\n\nnpm install --no-fund --no-audit && PORT=3080 REACT_APP_PUBLIC_KEY=$PUBLIC_KEY npm run start",
    "server": "cd server && #!/bin/sh\n\ngem install bundler && bundle install --quiet && ruby server.rb",
    "start": "npm install --no-fund --no-audit && node start.js"
  },
  "author": "Smartpay Co. Ltd.",
  "license": "MIT",
  "dependencies": {
    "boxen": "^1.3.0",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">= 14"
  }
}
